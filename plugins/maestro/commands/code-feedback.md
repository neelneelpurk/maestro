---
description: Review a PR (inline GitHub review) or the whole codebase (findings report + optional ready-for-agent tech-debt issues). Asks for scope + focus first.
argument-hint: "[pr#]"
---

Run the maestro **code-feedback** orchestration now: invoke the `maestro:code-feedback` skill with the Skill tool and follow it through to completion. Target PR, if given: $ARGUMENTS
