---
description: Drain the ready queue — implement every ready, assigned issue in dependency order on an integration branch, each per-issue PR auto-merged into it. You only coordinate.
argument-hint: ""
---

Run the maestro **drain** orchestration now: invoke the `maestro:drain` skill with the Skill tool and follow it through to completion. You are the coordinator — never merge the integration PR.
