---
description: Autonomous loop — build a roadmap (agent:auto issues that skip the ready-for-agent gate), then drain it on an integration branch, repeatedly until caught up (or the given goal is met). You only coordinate.
argument-hint: "[goal] [-- context]"
---

Run the maestro **auto** orchestration now: invoke the `maestro:auto` skill with the Skill tool and follow it through to completion. You are the coordinator — never implement, never merge. Goal/context, if given: $ARGUMENTS
