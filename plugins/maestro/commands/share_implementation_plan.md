---
description: Draft a test-first implementation plan for an issue and post it as a comment (no code, no branch, no PR).
argument-hint: "<issue#>"
---

Run the maestro **share-implementation-plan** orchestration for issue #`$ARGUMENTS`: invoke the `maestro:share-implementation-plan` skill with the Skill tool and follow it through to completion. Only draft and post the plan — do not implement, change labels, or open a PR.
