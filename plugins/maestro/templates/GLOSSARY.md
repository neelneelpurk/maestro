# maestro glossary

The vocabulary, labels, and state machine of the pipeline. Generated by
`/maestro:init`; the conventions in `.claude/rules/maestro.md` reference it.

## Branches & PRs

- **Default branch** (`main`/`master`) — the trunk. Only ever updated by a human merging an integration PR (or a `ship` PR).
- **Integration branch** (`maestro/integration-<stamp>`) — a per-run branch off the default branch where `drain`/`maestro:auto` land each issue. Created by `integration.sh start`.
- **Issue branch** (`maestro/issue-<n>-<slug>`) — one per issue, created with `gh issue develop` so GitHub records a native branch↔issue link. Branches off the integration branch (drain/auto) or the default branch (ship).
- **Integration PR** — integration branch → default branch. The **single human review gate**; never auto-merged. Accumulates a progress comment + checklist of integrated issues.
- **Per-issue PR** — issue branch → integration branch; auto-merged once green.
- **Ship PR** — issue branch → default branch, with `Closes #<n>`; awaits human review.

## Labels

### Type
| Label | Meaning |
|---|---|
| `maestro:prd` | A PRD / epic parent issue. |
| `maestro:roadmap` | A roadmap parent issue. |
| `maestro:bug` / `maestro:enhancement` | Category. |
| `maestro:tech-debt` | Technical debt to pay down. |

### Gate / mode
| Label | Meaning |
|---|---|
| `maestro:ready-for-agent` | Human-approved for autonomous implementation (the human gate). |
| `maestro:auto` | Autonomous lane — created by `/maestro:auto`; **skips the `maestro:ready-for-agent` gate**; picked automatically. |
| `maestro:hitl` | Needs a human; never auto-picked. |

### Execution state (one at a time)
| Label | Meaning |
|---|---|
| `maestro:in-progress` | A worker is implementing it. |
| `maestro:in-review` | `ship`: a PR to the default branch is open, awaiting your review. |
| `maestro:waiting-for-human-closure` | `drain`/`maestro:auto`: the work is merged into the integration branch; the issue stays open until you merge the integration PR and close it. |
| `maestro:blocked` | Board marker: has open dependencies. |

### PR
| Label | Meaning |
|---|---|
| `maestro:integration` | The integration → default-branch PR (your review gate). |

## State machine

```
created ──(human)── ready-for-agent ─┐        (supervised lane)
        └─(/maestro:auto)── auto ────────┤        (autonomous lane; skips the gate)
            assigned to me, unblocked  ▼
                       picked → in-progress
        /maestro:ship → PR → default branch → in-review            → you merge → closed
        drain/auto → PR → integration   → auto-merge (green)    → waiting-for-human-closure
                                          └ you merge the integration PR → bulk-close
        hitl ───────────────────────────────► never auto-picked (a human implements)
```

## Dependencies

Modeled with **native GitHub issue dependencies** (`blocked_by`). A blocker is
considered cleared once it is **closed** or labelled `maestro:waiting-for-human-closure`
(its work is already in the integration branch) — which is what lets the drain
queue advance without auto-closing issues.
